from function import fuzzy_search, get_all, semantic_search

index_name = "hr-policy-index"

# fuzzy_search(index_name, "Elasticsearch")

# get_all(index_name)

semantic_search(index_name, "พนักงานหญิงลาคลอดได้กี่วัน โดยไม่ได้รับค่าจ้่าง")